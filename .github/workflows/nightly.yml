name: C# Full Matrix Tests (Nightly)

on:
    schedule:
        - cron: "0 8 * * *" # 1AM PDT daily
    workflow_dispatch:

permissions:
    contents: read

jobs:
    full-matrix-tests:
        uses: ./.github/workflows/test.yml
        with:
            profile: full
        secrets: inherit
